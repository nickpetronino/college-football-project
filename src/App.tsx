import './App.css';
import {
    Button,
    Card,
    CardContent,
    Typography,
    Stack,
    Chip,
    Box,
    AppBar,
    Container,
    Toolbar,
} from '@mui/material';

import { Home, Group, Settings, Info } from '@mui/icons-material';

function App() {
    return (
        <Box
            mx={'auto'}
            maxWidth={'xl'}
        >
            Hello World
        </Box>
    );
}

export default App;
